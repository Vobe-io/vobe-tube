import React, { Component } from 'react';
import Style from 'style/settings.module.sass';

class Settings extends Component {
    render() {
        return(
            <div className={Style.Settings}>
                Settings
            </div>
        );
    }
}

export default Settings;